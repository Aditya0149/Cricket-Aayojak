import { HostNavDirective } from './host-nav.directive';

describe('HostNavDirective', () => {
  it('should create an instance', () => {
    const directive = new HostNavDirective();
    expect(directive).toBeTruthy();
  });
});
